{
  "name": "sort-id",
  "version": "0.0.3",
  "description": "Time sortable, type-prefixed IDs.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/ && tsc && esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js",
    "prepublishOnly": "npm run build",
    "test": "jest"
  },
  "repository": "callumthomson/sort-id",
  "author": "Callum Thomson",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "esbuild": "^0.23.0",
    "jest": "^29.7.0",
    "prettier": "^3.3.2",
    "tslib": "^2.6.3",
    "typescript": "^5.5.2"
  }
}
